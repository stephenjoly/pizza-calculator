name: Publish Staging Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push managed images
        env:
          GITHUB_SHA: ${{ github.sha }}
        run: |
          docker buildx build --platform linux/amd64 --tag ghcr.io/stephenjoly/pizza-calculator/pizza-calculator:latest --tag ghcr.io/stephenjoly/pizza-calculator/pizza-calculator:${GITHUB_SHA} --push '.'
